package authoring.views;

import java.util.Properties;

public interface AuthoringPane {
	
	/**
	 * Updates all the elements on the screen to the proper language
	 * @param language
	 */
	void setLanguage(Properties language);
	
}
