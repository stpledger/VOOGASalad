package authoring.factories;

/**
 * Enum that defines Clickable elements that can be created through the ElementFactory
 * @author Hemanth Yakkali(hy115)
 *
 */
public enum ClickElementType {
	Button,
	MenuItem,
	ComboBox,
}
